# Persistent storage

Runtime databases, model weights, logs and generated artifacts belong here
conceptually, but large/runtime files should live outside Git.

On Colab, Google Drive persistence is preferred by the current factory.
